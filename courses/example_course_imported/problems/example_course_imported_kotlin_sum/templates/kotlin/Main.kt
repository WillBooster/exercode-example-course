import java.util.Scanner

fun main() {
    val scanner = Scanner(System.`in`)
    val count = scanner.nextInt()
    var total = 0L
    for (index in 0 until count) {
        val value = scanner.nextInt()
        // TODO: valueをtotalに加えてください
    }
    println(total)
    scanner.close()
}
